<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:l="clr-namespace:WinTabberUI"
    xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:WinTabberUI.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
>
    <ContextMenu
        x:Shared="false"
        x:Key="SysTrayMenu"
        d:DataContext="{d:DesignInstance vm:NotifyIconViewModel, IsDesignTimeCreatable=False}"
    >
        <MenuItem Header="Show Window" Command="{Binding ShowWindowCommand}" />
        <MenuItem Header="Settings" Command="{Binding ShowSettingsCommand}" />
        <MenuItem
            Header="Enable Hooks"
            Command="{Binding PauseHooksCommand}"
            IsCheckable="True"
            IsChecked="{Binding AreHooksActive, Mode=OneWay}"
        />

        <MenuItem
            Header="Show theme colors"
            Command="{Binding SysColorsCommand}"
        />
        <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}" />
    </ContextMenu>
</ResourceDictionary>
