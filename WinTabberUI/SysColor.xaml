<Window x:Class="WinTabberUI.SysColor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinTabberUI"
        mc:Ignorable="d"
        Title="SysColor" Height="450" Width="800">
    <Window.DataContext>
        <local:SystemColorsViewModel/>
    </Window.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding SystemColorsList}" Background="Gray">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Color Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Color Value" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Rectangle Width="50" Height="20" Fill="{Binding Color}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
