<Window
    x:Class="WinTabberUI.SuspendedWindowsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:chrome="clr-namespace:WinTabber.UI.Common.Chrome;assembly=WinTabber.UI.Common"
    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    xmlns:vm="clr-namespace:WinTabberUI.ViewModels"
    mc:Ignorable="d"
    Title="SuspendedWindowsWindow"
    WindowStyle="None"
    AllowsTransparency="True"
    ShowInTaskbar="False"
    Topmost="True"
    ShowActivated="False"
    Focusable="False"
    SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    Background="#30000000"
    d:DataContext="{d:DesignInstance vm:SuspendedWindowsViewModel, IsDesignTimeCreatable=False}"
>
    <chrome:AcrylicChrome.AcrylicChrome>
        <chrome:AcrylicChrome
            AccentState="ACCENT_ENABLE_BLURBEHIND"
            CornerPreference="DWMWCP_ROUND"
            UnderStratumColor="#55ff0000"
        />
    </chrome:AcrylicChrome.AcrylicChrome>
    <Window.Resources>
        <Style x:Key="ResumeTileButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="12, 8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="4"
                        >
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22666666" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#dd666666" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <ItemsControl ItemsSource="{Binding Items}" Margin="10">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate DataType="vm:SuspendedWindowItemViewModel">
                <Button
                    Style="{StaticResource ResumeTileButton}"
                    Command="{Binding ResumeCommand}"
                    ToolTip="Click to resume"
                >
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Sun" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding ProcessName}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Title}" Opacity="0.8" MaxWidth="220" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </StackPanel>
                </Button>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</Window>
