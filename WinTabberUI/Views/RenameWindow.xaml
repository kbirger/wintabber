<Window x:Class="WinTabberUI.RenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinTabberUI"
        mc:Ignorable="d"
        Title="RenameWindow" 
        Height="100" 
        Width="300" 
        Background="{DynamicResource  ResourceKey={x:Static SystemColors.WindowBrushKey}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label>Current:</Label>
        <TextBlock Grid.Column="1" Text="{Binding WindowItem.Title}"/>
        <Label Grid.Row="1">New:</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Mode=TwoWay, Path=NewTitle}"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right">
            <Button Click="Button_Click">Rename</Button>
            <Button Click="Button_Click_1">Cancel</Button>
            
        </StackPanel>
    </Grid>
</Window>
