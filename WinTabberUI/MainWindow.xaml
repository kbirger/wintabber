<Window x:Class="WinTabberUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinTabberUI"
        mc:Ignorable="d"
        Title="MainWindow"
        WindowStartupLocation="CenterScreen"
        Visibility="Hidden"
        Name="Foo"
        >
    <Window.Resources>
        
    </Window.Resources>
    <Border BorderThickness="1" CornerRadius="20" Background="Transparent" Padding="20" >
        <ListView 
                x:Name="TabListView" 
                SelectionMode="Single" 
                PreviewMouseDown="TabListView_MouseDown"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                SelectionChanged="TabListView_SelectionChanged"
                PreviewKeyDown="TabListView_KeyDown"
                ItemsSource="{Binding WindowItems}" 
                SelectedIndex="{Binding Mode=TwoWay, Path=SelectedIndex}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel 
                        Orientation="Horizontal" 
                        KeyboardNavigation.DirectionalNavigation="None"
                        ItemWidth="{Binding MaxItemHeight}" 
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Title}" FontSize="16" Margin="0 0 0 10" Padding="0" Grid.Row="0" TextWrapping="NoWrap" MaxWidth="400" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                        <Viewbox Grid.Row="1"  HorizontalAlignment="Center" VerticalAlignment="Top" Stretch="Uniform">
                            <local:WindowThumbnail Source="{Binding Path=Handle}" />
                        </Viewbox>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
</Window>
