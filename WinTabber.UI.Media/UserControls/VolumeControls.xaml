
<UserControl
    x:Class="WinTabber.UI.Media.UserControls.VolumeControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WinTabber.UI.Media.UserControls"
    mc:Ignorable="d"
    xmlns:b="clr-namespace:WinTabber.UI.Common.Behaviors;assembly=WinTabber.UI.Common"
    d:DesignHeight="42"
    d:DesignWidth="250"
    xmlns:vm="clr-namespace:WinTabber.UI.Media.ViewModels"
    d:DataContext="{d:DesignInstance vm:VolumeControlsViewModel, IsDesignTimeCreatable=False}"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WinTabber.UI.Media;component/Resources/Resources.xaml" />
                <ResourceDictionary Source="/WinTabber.UI.Common;component/Resources/ValueConvertersResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Slider
            Minimum="0"
            Maximum="100"
            TickFrequency="5"
            AutoToolTipPlacement="TopLeft"
            AutoToolTipPrecision="0"
            IsMoveToPointEnabled="true"
            b:HintBehavior.HintText="{Binding VolumeHintText}"
            IsEnabled="{Binding CanSetVolume}"
            Value="{Binding Volume, Converter={StaticResource FloatToPercentageConverter}, Mode=TwoWay}"
        />
        <ToggleButton
            Style="{StaticResource MuteToggle}"
            Margin="7 0 0 0"
            Grid.Column="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch"
            IsChecked="{Binding IsMuted, Mode=OneWay}"
            Command="{Binding SetMuted}"
            CommandParameter="{Binding IsMuted, Converter={StaticResource InverseBoolConverter}}"
            b:HintBehavior.HintText="{Binding MuteHintText}"
        >
            <!--<mdi:MaterialIconExt Kind="VolumeOff"/>-->
        </ToggleButton>
    </Grid>
</UserControl>
