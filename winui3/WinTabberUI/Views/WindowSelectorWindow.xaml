<winuiex:WindowEx
    x:Class="WinTabberUI.Views.WindowSelectorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winuiex="using:WinUIEx"
    xmlns:controls="using:WinTabberUI.Controls"
    xmlns:views="using:WinTabberUI.Views"
    xmlns:vm="using:WinTabber.ViewModels"
    xmlns:wrap="using:CommunityToolkit.WinUI.Controls"
    xmlns:c="using:WinTabber.UI.Common.ValueConverters"
    Title="WindowSelector"
    IsShownInSwitchers="False"
    IsAlwaysOnTop="True"
    IsTitleBarVisible="False"
    IsResizable="False"
>
    <!-- Resources live on the root Grid: Microsoft.UI.Xaml.Window has no Resources property. -->
    <Grid x:Name="RootGrid" Background="#01000000" PreviewKeyDown="OnRootGridPreviewKeyDown">
        <Grid.Resources>
            <c:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <c:DimIfTrueConverter x:Key="DimIfTrueConverter" />
        </Grid.Resources>

        <controls:SpatialNavigationListView
            x:Name="TabListView"
            SelectionMode="Single"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ItemsSource="{x:Bind ViewModel.WindowItems, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
        >
            <controls:SpatialNavigationListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <wrap:WrapPanel
                        Orientation="Horizontal"
                        HorizontalSpacing="8"
                        VerticalSpacing="8"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                    />
                </ItemsPanelTemplate>
            </controls:SpatialNavigationListView.ItemsPanel>
            <controls:SpatialNavigationListView.ItemTemplate>
                <DataTemplate x:DataType="vm:WindowItem">
                    <Grid
                        x:Name="TileRootGrid"
                        Margin="0"
                        Background="Transparent"
                        Opacity="{Binding IsDimmed, Converter={StaticResource DimIfTrueConverter}}"
                        PointerPressed="OnTileGridPointerPressed"
                    >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <views:EditableTextBlock ViewModel="{x:Bind}" />
                        <Viewbox
                            Grid.Row="1"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Stretch="Uniform"
                            Margin="0,10,0,0"
                        >
                            <controls:WindowThumbnail Source="{x:Bind Handle, Mode=OneWay}" />
                        </Viewbox>
                    </Grid>
                </DataTemplate>
            </controls:SpatialNavigationListView.ItemTemplate>
        </controls:SpatialNavigationListView>

        <!-- Visibility uses classic Binding (DataContext set in code-behind), not x:Bind: an
             x:Bind+Converter here fails CS1503 (WindowEx is not a FrameworkElement). Segoe Fluent
             "Dismiss" glyph (U+E711) below replaces the WPF original's hand-drawn Path glyph. -->
        <Button
            x:Name="CloseApplicationButton"
            Command="{x:Bind ViewModel.CloseApplicationCommand, Mode=OneWay}"
            ToolTipService.ToolTip="Close all windows of this application"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Margin="0"
            Visibility="{Binding IsCloseApplicationButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}"
        >
            <FontIcon Glyph="&#xE711;" />
        </Button>
    </Grid>
</winuiex:WindowEx>
