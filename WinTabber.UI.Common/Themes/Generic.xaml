<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WinTabber.UI.Common.Controls"
    >

    <SolidColorBrush x:Key="ShortcutChipForegroundBrush" Color="#202020" />
    <SolidColorBrush x:Key="ShortcutCaptureBoxBorderBrush" Color="#6E7683" />
    <SolidColorBrush x:Key="ShortcutHintForegroundBrush" Color="#99FFFFFF" />
    <SolidColorBrush x:Key="ShortcutEmptyForegroundBrush" Color="#88808080" />
    <SolidColorBrush x:Key="ShortcutValidationForegroundBrush" Color="#FFC42B1C" />
    <SolidColorBrush x:Key="ShortcutValidationBannerBackgroundBrush" Color="#33C42B1C" />
    <SolidColorBrush x:Key="ShortcutGroupBackgroundBrush" Color="#3A3A3A" />
    <SolidColorBrush x:Key="ShortcutGroupHoverBackgroundBrush" Color="#454545" />
    <SolidColorBrush x:Key="ShortcutGroupBorderBrush" Color="#55FFFFFF" />

    <!-- The whole combination is one clickable frame — matches Flow.Launcher's HotkeyControl,
         where clicking the shortcut itself (not a separate pencil icon) opens the capture dialog. -->
    <Style x:Key="ShortcutEditButtonStyle" TargetType="Button">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Bg"
                        Padding="8,5"
                        Background="{DynamicResource ShortcutGroupBackgroundBrush}"
                        BorderBrush="{DynamicResource ShortcutGroupBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource ShortcutGroupHoverBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Chip rendering exists in exactly one place. ShortcutCaptureBox hosts a ShortcutPresenter
         rather than duplicating this template. Every chip in a combo — modifiers included — shares
         the same accent fill, matching Flow.Launcher's HotkeyControl (every key tile uses
         AccentButtonBackground, not just the trigger key). The lighter tint softens the OS accent
         color so the pill does not read as one flat saturated block. -->
    <DataTemplate x:Key="ShortcutChipTemplate">
        <Border
            Margin="0,0,6,0"
            Padding="10,5"
            BorderThickness="1"
            CornerRadius="6">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.AccentColorLight2BrushKey}}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.AccentColorLight2BrushKey}}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Kind}" Value="Hint">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="Padding" Value="2,5" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock FontSize="13" Text="{Binding Text}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource ShortcutChipForegroundBrush}" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Kind}" Value="Hint">
                                <Setter Property="Foreground" Value="{DynamicResource ShortcutHintForegroundBrush}" />
                                <Setter Property="FontStyle" Value="Italic" />
                                <Setter Property="FontWeight" Value="Normal" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>
    </DataTemplate>

    <!-- Large-scale chip, shared by the capture dialog's idle and in-progress display — mirrors
         Flow.Launcher's HotkeyControlDialog key tiles (50x50 minimum, bold 18pt). -->
    <DataTemplate x:Key="ShortcutChipTemplateLarge">
        <Border
            MinWidth="50"
            MinHeight="50"
            Margin="5,0,5,0"
            Padding="8"
            BorderThickness="1"
            CornerRadius="6">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.AccentColorLight2BrushKey}}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.AccentColorLight2BrushKey}}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Kind}" Value="Hint">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="18"
                Text="{Binding Text}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource ShortcutChipForegroundBrush}" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Kind}" Value="Hint">
                                <Setter Property="Foreground" Value="{DynamicResource ShortcutHintForegroundBrush}" />
                                <Setter Property="FontStyle" Value="Italic" />
                                <Setter Property="FontWeight" Value="Normal" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>
    </DataTemplate>

    <Style x:Key="ShortcutPresenterLargeStyle" TargetType="{x:Type controls:ShortcutPresenter}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ShortcutPresenter}">
                    <Grid>
                        <ItemsControl
                            x:Name="PART_Chips"
                            ItemTemplate="{StaticResource ShortcutChipTemplateLarge}"
                            ItemsSource="{TemplateBinding Chips}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type controls:ShortcutPresenter}}}" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                        <TextBlock
                            x:Name="PART_Empty"
                            VerticalAlignment="Center"
                            FontSize="14"
                            FontStyle="Italic"
                            Foreground="{DynamicResource ShortcutEmptyForegroundBrush}"
                            Text="{TemplateBinding EmptyText}"
                            Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEmpty" Value="True">
                            <Setter TargetName="PART_Empty" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_Chips" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Used only inside ShortcutCaptureDialog (WinTabberUI/Views) — the row's own capture box
         above stays at its normal size. -->
    <Style x:Key="ShortcutCaptureBoxDialogStyle" TargetType="{x:Type controls:ShortcutCaptureBox}">
        <Setter Property="Focusable" Value="True" />
        <Setter Property="MinHeight" Value="100" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ShortcutCaptureBox}">
                    <StackPanel HorizontalAlignment="Center">
                        <controls:ShortcutPresenter
                            x:Name="PART_Presenter"
                            HorizontalAlignment="Center"
                            EmptyText="Press a shortcut…"
                            Style="{StaticResource ShortcutPresenterLargeStyle}"
                            Trigger="{Binding Trigger, RelativeSource={RelativeSource TemplatedParent}}" />

                        <ItemsControl
                            x:Name="PART_Capturing"
                            HorizontalAlignment="Center"
                            ItemTemplate="{StaticResource ShortcutChipTemplateLarge}"
                            ItemsSource="{TemplateBinding PendingChips}"
                            Visibility="Collapsed">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <TextBlock
                            x:Name="PART_Hint"
                            Margin="0,12,0,0"
                            HorizontalAlignment="Center"
                            FontStyle="Italic"
                            Foreground="{DynamicResource ShortcutHintForegroundBrush}"
                            Text="Press a shortcut… (Esc to cancel)"
                            Visibility="Collapsed" />

                        <TextBlock
                            x:Name="PART_Validation"
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource ShortcutValidationForegroundBrush}"
                            Text="{TemplateBinding ValidationMessage}"
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsCapturing" Value="True">
                            <Setter TargetName="PART_Presenter" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_Capturing" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_Hint" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <DataTrigger Binding="{Binding ValidationMessage, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                            <Setter TargetName="PART_Validation" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:ShortcutPresenter}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ShortcutPresenter}">
                    <Grid>
                        <ItemsControl
                            x:Name="PART_Chips"
                            ItemTemplate="{StaticResource ShortcutChipTemplate}"
                            ItemsSource="{TemplateBinding Chips}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type controls:ShortcutPresenter}}}" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                        <TextBlock
                            x:Name="PART_Empty"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Foreground="{DynamicResource ShortcutEmptyForegroundBrush}"
                            Text="{TemplateBinding EmptyText}"
                            Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEmpty" Value="True">
                            <Setter TargetName="PART_Empty" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_Chips" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:ShortcutCaptureBox}">
        <Setter Property="Focusable" Value="True" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ShortcutCaptureBox}">
                    <Border
                        Padding="6,4"
                        Background="Transparent"
                        BorderBrush="{DynamicResource ShortcutCaptureBoxBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <StackPanel>
                            <controls:ShortcutPresenter
                                x:Name="PART_Presenter"
                                Trigger="{Binding Trigger, RelativeSource={RelativeSource TemplatedParent}}" />

                            <StackPanel x:Name="PART_Capturing" Orientation="Horizontal" Visibility="Collapsed">
                                <ItemsControl
                                    ItemTemplate="{StaticResource ShortcutChipTemplate}"
                                    ItemsSource="{TemplateBinding PendingChips}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontStyle="Italic"
                                    Foreground="{DynamicResource ShortcutEmptyForegroundBrush}"
                                    Text="Press a shortcut… (Esc to cancel)" />
                            </StackPanel>

                            <TextBlock
                                x:Name="PART_Validation"
                                FontSize="11"
                                Foreground="{DynamicResource ShortcutValidationForegroundBrush}"
                                Text="{TemplateBinding ValidationMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsCapturing" Value="True">
                            <Setter TargetName="PART_Capturing" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_Presenter" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <DataTrigger Binding="{Binding ValidationMessage, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                            <Setter TargetName="PART_Validation" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
